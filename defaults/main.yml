---
# defaults file for ansible-pull
ansible_pull_timer: |
  OnBootSec=5m
  OnCalendar=daily
  RandomizedDelaySec=30m
ansible_pull_url: https://github.com/<user>/ansible-pull.git

templates:
  - ansible-pull.service
  - ansible-pull.timer
